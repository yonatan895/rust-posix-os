name: Quality

on:
  workflow_call:

jobs:
  rust-quality:
    name: rustfmt + clippy -D warnings
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Rust nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt, clippy, rust-src
      - uses: Swatinem/rust-cache@v2
      - name: rustfmt (changed .rs only)
        if: github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'ci:full')
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          set -euo pipefail
          git fetch origin "$BASE_REF" --depth=1
          files=$(git diff --name-only --diff-filter=ACMR \
            "origin/$BASE_REF...HEAD" | grep '\.rs$' || true)
          if [ -z "$files" ]; then
            echo "No Rust files changed; skip rustfmt."
            exit 0
          fi
          echo "$files" | xargs rustfmt --edition 2024 --check
      - name: rustfmt (full tree)
        if: github.event_name != 'pull_request' || contains(github.event.pull_request.labels.*.name, 'ci:full')
        run: cargo fmt --all -- --check
      - name: clippy host crates
        run: cargo clippy -p posix-abi -p mm-core -p xtask --all-targets -- -D warnings
      - name: clippy kernel
        env:
          RUSTFLAGS: "-C relocation-model=static -C code-model=kernel"
        run: |
          cargo clippy -p kernel \
            --target x86_64-unknown-none \
            -Zbuild-std=core,compiler_builtins,alloc \
            -Zbuild-std-features=compiler-builtins-mem \
            -- -D warnings
      - name: clippy userland
        env:
          RUSTFLAGS: "-C relocation-model=static"
        run: |
          cargo clippy -p libc -p shell -p init -p coreutils \
            --target x86_64-unknown-none \
            -Zbuild-std=core,compiler_builtins,alloc \
            -Zbuild-std-features=compiler-builtins-mem \
            -- -D warnings
      - name: doc coverage host crates
        env:
          RUSTDOCFLAGS: "-D missing-docs -D warnings"
        run: cargo doc --no-deps -p posix-abi -p mm-core -p xtask
      - name: doc coverage kernel
        env:
          RUSTDOCFLAGS: "-D missing-docs -D warnings"
          RUSTFLAGS: "-C relocation-model=static -C code-model=kernel"
        run: |
          cargo doc --no-deps -p kernel \
            --target x86_64-unknown-none \
            -Zbuild-std=core,compiler_builtins,alloc \
            -Zbuild-std-features=compiler-builtins-mem
      - name: doc coverage userland
        env:
          RUSTDOCFLAGS: "-D missing-docs -D warnings"
          RUSTFLAGS: "-C relocation-model=static"
        run: |
          cargo doc --no-deps -p libc -p shell -p init -p coreutils \
            --target x86_64-unknown-none \
            -Zbuild-std=core,compiler_builtins,alloc \
            -Zbuild-std-features=compiler-builtins-mem
