[workspace]
members = [
    "kernel",
    "libs/posix-abi",
    "libs/mm-core",
    "libs/libc",
    "userland/init",
    "userland/shell",
    "userland/coreutils",
    "tools/xtask",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
authors = ["Rust POSIX OS Developers"]
license = "MIT OR Apache-2.0"

[workspace.lints.clippy]
incompatible_msrv = "allow"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = 3
codegen-units = 1
